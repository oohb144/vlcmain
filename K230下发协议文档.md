# K230 下发指令协议文档（上位机播放器 → MaixCAM）

## 一、总览

上位机播放器（Flutter）通过 **HTTP POST** 向 K230 下发指令，K230 端需在现有 `status_server.py`
（当前只有 `GET /status`）基础上**新增一个 POST 路由**来接收并执行。

- 请求方法：`POST`
- 路径：`/command`
- 端口：与 `/status` 同一个 server，默认 **8001**（`status_server.py` 构造函数默认 `port=8001`，
  `config.py` `STATUS_SERVER_PORT=8001`）
- Content-Type：`application/json`
- 请求体：UTF-8 JSON，**所有指令都至少含一个 `cmd` 字符串字段**，可选附带参数。
- 响应：K230 应返回 JSON `{"ok": true/false, "msg": "可读说明"}`，HTTP 状态码建议 200（成功）或 400（指令无效）。
  上位机仅根据 `ok` 与 `msg` 做 SnackBar 反馈。

实现要点：
- 沿用现有 `status_server.py` 的 socket 解析逻辑，在请求行判断中新增 `if request.startswith("POST /command")` 分支。
- 解析 `Content-Length`，从 body 读取 JSON；解析失败返回 `{"ok": false, "msg": "bad json"}`。
- 按 `cmd` 字段分发到 `main.py` 中已有的动作函数/状态机切换（下表"对应 K230 动作"给出代码位置与现有触发点可直接复用）。

---

## 二、上行配合：GET /status（已存在，无需改）

上位机轮询 `GET /status` 获取以下字段（`status_server.py:57-65`），用于状态栏展示：

```json
{
  "state": "空闲",              // 中文状态文案
  "has_face": false,
  "face_count": 0,              // 人员数量
  "known_face_count": 0,
  "unknown_face_count": 0,
  "face_labels": [],
  "recording": false,
  "record_duration": 0
}
```

下发指令后，上位机会在下一次轮询（默认 1s）看到状态/开关变化。建议开关类指令在 K230 端写入
`app_state` 对应字段后立即返回 `ok:true`，UI 由轮询自然校正。

---

## 三、下发指令清单（按分组）

### 1. 主操作（识别/录入/录制三组开关 + 动作）

| cmd | 额外字段 | 对应 K230 动作 / 代码位置 |
|---|---|---|
| `start_recognize` | — | 串口 0x04 路径：`IDLE → RECOGNIZING`，`main.py:781-784`，`state_machine.transition(State.RECOGNIZING)` |
| `stop_recognize` | — | 串口 0x06 路径：`RECOGNIZING/RECORDING/MANUAL_RECORDING/ENROLLING → IDLE`，`main.py:787-792` |
| `start_enroll` | — | 串口 0x05 路径：`IDLE → ENROLLING` + push 录入页，`main.py:773-778` |
| `stop_enroll` | — | 等价 0x06（录入态退出），转入 IDLE |
| `start_record` | — | 触摸"录制"：`IDLE → MANUAL_RECORDING`，`main.py:490-501` |
| `stop_record` | — | 等价 0x06（录制态退出） |
| `enroll_face` | — | 执行人脸采集保存 `_do_enroll_face()`，`main.py:1496-1576` |
| `go_home` | — | 串口 0x01：弹所有子页 + 转 IDLE，`main.py:751-756` |

### 2. 页面跳转

| cmd | 额外字段 | 对应 K230 动作 |
|---|---|---|
| `go_settings` | — | 串口 0x02：push 设置页，`main.py:759-762` |
| `go_enroll_page` | — | 串口 0x03：停状态 + push 录入页，`main.py:765-770` |
| `fusion_page` | — | 触摸"融合"：停状态 + push 融合播放页，`main.py:522-528`、`main.py:640-645`（现仅语音触发，缺串口指令，需新增分发） |
| `recordings_page` | — | 仅语音"recordings"触发，`main.py:636-638`（缺串口/外部指令，需新增分发） |

> 注：`fusion_page`、`recordings_page` 在 K230 端目前没有串口指令，需在 POST /command 分发中直接调用对应 UI 入口代码。

### 3. 设备开关（bool 值，value=true 开 / false 关）

| cmd | value | 对应 K230 动作 / 字段 |
|---|---|---|
| `http_stream` | bool | 串口 0x07/0x08：`stream_manager.stop()/start()`，置 `app_state.stream_enable`，`main.py:795-807` |
| `rtsp` | bool | 设置页同步：`_rtsp_manager.start()/stop()`，`main.py:964-993`；同时置 `app_state.rtsp_enable` 持久化 |
| `audio` | bool | 置 `app_state.audio_enable` 即时生效，`ui_pages.py:474-475` |
| `led` | bool | 置 `app_state.led_enable` 即时生效，`ui_pages.py:478-479` |
| `voice` | bool | `_voice_recognition.start()/stop()`，`main.py:508-520` |
| `auto_record` | bool | 置 `app_state.auto_record_enable`，`main.py:530-543` |

### 4. 阈值调节（float，范围 0.10–0.90，启动可通过 GET 参数回读当前值）

| cmd | 额外字段 | 对应 K230 动作 |
|---|---|---|
| `set_threshold` | `key="conf_threshold"`, `value=0.25` | `_face_detector.set_detect_threshold()`，`main.py:948-953` `_check_threshold_update` |
| `set_threshold` | `key="recognize_threshold"`, `value=0.72` | 识别阈值，`main.py:948-955`；持久化保存到应用配置 |

### 5. 危险操作（需 K230 端二次确认或直接执行）

| cmd | 对应 K230 动作 |
|---|---|
| `clear_faces` | `_face_detector.clear_all_faces()`，`main.py:818-820` |
| `clear_recordings` | `_on_clear_recordings()` 清录像目录，`main.py:867-888` |
| `exit_app` | `app.set_exit_flag(True)`，`main.py:1069` / `ui_pages.py:505` |

---

## 四、请求/响应示例

**示例 1：开启 RTSP 推流**
```
POST http://<k230_ip>:8001/command HTTP/1.1
Content-Type: application/json

{"cmd": "rtsp", "value": true}
```
响应：
```json
{"ok": true, "msg": "rtsp on"}
```

**示例 2：设置检测阈值**
```
POST /command
{"cmd": "set_threshold", "key": "conf_threshold", "value": 0.30}
```
响应：
```json
{"ok": true, "msg": "conf_threshold=0.30"}
```

**示例 3：未知指令**
```json
{"ok": false, "msg": "unknown cmd: reboot"}
```
（上位机会用红色 SnackBar 显示 msg）

---

## 五、推荐实现结构（伪代码，供 K230 端参考）

在 `status_server.py` 的请求处理里，紧挨现有 `if request.startswith('GET /status'):` 之后加：

```python
if request.startswith('POST /command'):
    # 解析 Content-Length 与 body
    body = ...  # 从 raw request 读 JSON 字符串
    try:
        payload = json.loads(body)
    except Exception:
        return _json_resp({"ok": False, "msg": "bad json"}, code=400)

    cmd = payload.get('cmd')
    ok, msg = dispatch_command(cmd, payload)   # 你自己实现的核心分发函数
    return _json_resp({"ok": ok, "msg": msg}, code=200 if ok else 400)
```

`dispatch_command` 内部按 `cmd` 调度到上文"对应 K230 动作"列出的现成函数；对当前缺串口指令的
`fusion_page` / `recordings_page`，直接复用 `main.py` 里触摸/语音触发时调的 UI 入口即可，无需新增串口字节。

---

## 六、RTSP 地址如何告诉播放器

K230 端 `stream_manager.py:1914` 用 `self._server.get_url()` 取 RTSP URL，并把 `0.0.0.0` 替换为真实 IP。
建议把该 URL 也放进 `GET /status` 的返回（例如加一个 `"rtsp_url": "rtsp://192.168.x.x:..."` 字段），
播放器首轮轮询拿到后可自动填入"RTSP 地址"——这样你只需在播放器设置里填 K230 的 IP+8001 状态接口，
RTSP 地址就能自动获取，省去手动复制。

播放器端已预留对 `raw` 兜底字段的展示，新增 `rtsp_url` 字段无需改播放器代码即可在状态栏显示；
（若要自动填入输入框，再小改 `settings_page`/`player_page` 即可，后续你再决定。）
```